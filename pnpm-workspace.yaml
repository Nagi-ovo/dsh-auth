allowBuilds:
  # Optional postinstall-only helpers (pi-ai transitive deps): nothing at
  # runtime needs their build scripts; refusing keeps installs hermetic.
  '@google/genai': false
  protobufjs: false
